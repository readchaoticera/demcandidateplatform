# GitHub Pages site

Two static pages built from a completed collection run:

| File | What it is |
|---|---|
| `index.html` | The tracker: searchable, filterable table of all 432 candidates |
| `analysis.html` | The written analysis: headline findings, method, limits |
| `styles.css` | Chaotic Era brand tokens and primitives |
| `assets/logo.png` | Chaotic Era mark, used in the masthead |
| `data.json` | The Explorer's payload, regenerated by `scripts/build_site.py` |

## Branding

`styles.css` mirrors the Chaotic Era YouTube Politics Leaderboard so the two
dashboards read as one publication: the same tokens (`--accent` #419eff,
`--ink` #0d1117, `--paper` #fafcff), the same IBM Plex Serif / Inter pairing,
the same masthead with a 3px accent rule, 12px cards and 999px pills. Keep this
block in sync with that site.

The Artifact copy of the analysis is generated by `scripts/build_artifact.py`,
which inlines `styles.css`, inlines the logo as a data URI and strips the
document skeleton, because an Artifact is published as a single file behind a
strict CSP that blocks external images.

## Rebuilding after a new run

```bash
dcp run --as-of $(date +%Y-%m-%d)
python scripts/build_site.py
```

`build_site.py` reads `data/out/roster.json` and `data/out/analysis.json` and
rewrites `docs/data.json`. The dashboard picks up the new numbers on reload —
the stat tiles, filter counts and footer all derive from the payload, so
nothing in `index.html` needs editing.

## Working on it locally

The page fetches `data.json` over HTTP, so it must be served rather than opened
from disk:

```bash
python3 -m http.server -d docs 8000
```
